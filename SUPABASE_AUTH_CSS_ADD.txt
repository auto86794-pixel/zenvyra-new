
/* =========================================================
   SUPABASE AUTH – kiegészítések
   ========================================================= */

.auth-loading {
  min-height: 100vh;
  display: grid;
  place-content: center;
  justify-items: center;
  gap: 14px;
  background:
    radial-gradient(circle at 35% 30%, rgba(255, 179, 157, 0.32), transparent 28%),
    radial-gradient(circle at 70% 72%, rgba(185, 153, 231, 0.28), transparent 28%),
    #fff9f7;
  color: var(--ink);
  font-size: 24px;
  font-weight: 850;
  letter-spacing: 0.24em;
}

.auth-loading-mark {
  width: 68px;
  height: 68px;
  border-radius: 24px;
  display: grid;
  place-items: center;
  color: white;
  font-size: 30px;
  background: linear-gradient(145deg, var(--coral), var(--purple));
  box-shadow: 0 16px 36px rgba(138, 92, 204, 0.2);
}

.login-button:disabled,
.social-button:disabled,
.guest-button:disabled,
.auth-tabs button:disabled,
.text-link:disabled,
.register-row button:disabled,
.eye-button:disabled {
  cursor: default;
  opacity: 0.62;
}

.auth-message.success {
  background: #f4eefc;
  color: #7049a7;
}
